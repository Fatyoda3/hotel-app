version: "3.9"
services:
  server:
    container_name: "hotel-api"
    ports:
      - "3000-8080"
    environment:
      HOTEL_MONGO_URL: mongodb://mongo:27017/hoteldb
    depends_on:
      - mongodb

  mongodb:
    image: mongo:7


